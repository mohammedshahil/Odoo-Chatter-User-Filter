# Chatter User Filter

Module that enhances the chatter functionality by adding user-based filtering capabilities.

## Overview

The Chatter User Filter module adds a dropdown filter to the chatter interface, allowing users to filter messages by specific authors. This makes it easier to focus on conversations from particular users, especially in threads with many participants.
